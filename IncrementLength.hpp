/*
    IncrementLength.hpp

    Include file for function to increment the length of a string
*/

#ifndef INCREMENT_LENGTH_HPP
#define INCREMENT_LENGTH_HPP

#include <string_view>

// increment the length of the string
int incrementLength(std::string_view s);

#endif
